#include <bits/stdc++.h>

using namespace std;

int sum(vector<int>b){
    int sum=0;
    for(int i=0;i<b.size();i++){
        sum+=b[i];
    }
    return sum;
}
int main(){
    int n;
    cin>>n;
    vector<int>A(n);
    for(int i=0;i<n;i++){
        cin>>A[i];
    }
    cout<<sum(A);

}
